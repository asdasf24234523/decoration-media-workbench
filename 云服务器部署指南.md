# 云服务器部署指南 — 装修新媒体协同工作台

## 秒杀服务器后，第一步做什么

### 需要准备的信息
- 服务器公网 IP（例如：1.2.3.4）
- 服务器 root 密码（或 SSH 密钥）

---

## 第一阶段：服务器初始化（约 5 分钟）

### 1. 用 SSH 连接服务器
在 Mac 终端执行：
```bash
ssh root@你的服务器IP
# 输入密码即可登录
```

### 2. 修改 root 密码（可选）
```bash
passwd
```

### 3. 开放防火墙端口
```bash
# 开放 3000（后端服务） 和 80/443（将来域名用）
ufw allow 22    # SSH
ufw allow 3000  # 后端服务
ufw allow 80    # HTTP
ufw allow 443   # HTTPS
ufw enable
```

### 4. 安装基础工具
```bash
apt update && apt install -y curl wget git
```

---

## 第二阶段：部署应用（约 10 分钟）

### 方案 A：一键脚本（推荐）

把 `workbench/server/deploy.sh` 上传到服务器执行：

```bash
# 在本机（Mac）上传脚本到服务器
scp /Users/picha/.qclaw/workspace-3010ll10ltqof1lk/workbench/server/deploy.sh root@你的服务器IP:/root/

# 在服务器执行
chmod +x /root/deploy.sh
/root/deploy.sh
```

### 方案 B：手动部署（更可控）

**Step 1 — 安装 Node.js 18**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs
node -v  # 应显示 v18.x.x
```

**Step 2 — 安装 MySQL**
```bash
apt-get install -y mysql-server
systemctl enable mysql
systemctl start mysql
mysql --version  # 应显示 8.0.x
```

**Step 3 — 创建数据库**
```bash
mysql
```
在 mysql 里执行：
```sql
CREATE DATABASE media_workbench CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'workbench'@'localhost' IDENTIFIED BY 'WbPass2026!';
GRANT ALL PRIVILEGES ON media_workbench.* TO 'workbench'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

**Step 4 — 上传代码**
方式一（从 GitHub 拉，服务器需要有 SSH key）：
```bash
cd /root
git clone <你的仓库地址> media-workbench
cd media-workbench/server
```
方式二（用 scp 从本机传）：
```bash
# 在本机 Mac 终端执行
scp -r /Users/picha/.qclaw/workspace-3010ll10ltqof1lk/workbench/server root@你的服务器IP:/root/media-workbench/
```

**Step 5 — 安装依赖并配置**
```bash
cd /root/media-workbench/server
npm install

# 创建 .env
cat > .env << 'EOF'
PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=workbench
DB_PASSWORD=WbPass2026!
DB_NAME=media_workbench
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_STRING_AT_LEAST_32_CHARS
STATIC_DIR=/root/media-workbench
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
EOF
```

**Step 6 — 启动**
```bash
npm start &
sleep 3
curl http://localhost:3000/health
# 应返回 {"ok":true}
```

**Step 7 — 验证**
在浏览器打开：`http://服务器IP:3000`
用 admin / admin123 登录

---

## 第三阶段：配置域名（可选）

假设你的域名是 `zimaptoop.asia`，想用 `app.zimaptoop.asia` 访问：

### 1. 域名实名认证（DNSPod）
登录 dnspod.cn → 找到 `zimaptoop.asia` → 完成实名认证

### 2. 添加 DNS 解析
在 DNSPod 添加 A 记录：
| 主机记录 | 记录类型 | 记录值 |
|---|---|---|
| app | A | 你的服务器IP |

### 3. 用 Nginx 反向代理（让域名自动转 https）
```bash
# 安装 Nginx
apt install -y nginx

# 配置
cat > /etc/nginx/sites-available/workbench << 'EOF'
server {
    listen 80;
    server_name app.zimaptoop.asia;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
EOF

ln -s /etc/nginx/sites-available/workbench /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

### 4. 后续可选：Let's Encrypt 免费 SSL 证书
```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d app.zimaptoop.asia
```

---

## 钉钉 H5 微应用配置（待域名配置完成后）

1. 登录 [钉钉开放平台](https://open.dingtalk.com/)
2. 创建应用 → H5 微应用
3. 填写：
   - 应用首页地址：`https://app.zimaptoop.asia`
   - 可信域名：`app.zimaptoop.asia`
4. 配置 JSAPI 鉴权（后端已有 `/api/dingtalk/config` 接口占位）

---

## 常见问题

**Q：服务重启后要手动启动？**
用 systemd 管理：
```bash
cat > /etc/systemd/system/media-workbench.service << 'EOF'
[Unit]
Description=Media Workbench
After=network.target mysql.service

[Service]
ExecStart=/usr/bin/node /root/media-workbench/server/server.js
WorkingDirectory=/root/media-workbench/server
Restart=always

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable media-workbench
systemctl start media-workbench
```

**Q：服务器 IP 是多少？**
在阿里云/腾讯云控制台 → 云服务器 → 实例 → 公网 IP

**Q：scp 上传要密码？**
```bash
# 在服务器先安装 openssh-server
apt install -y openssh-server
# 本机 Mac 执行 scp 时输入 root 密码即可
```
